name: FTP Deploy to InfinityFree

on:
  push:
    branches:
      - main   # deploy when pushing to main branch

jobs:
  ftp-deploy:
    runs-on: ubuntu-latest
    environment: FTP_HOST   # must match your environment name exactly

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node.js (required by FTP Deploy action)
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: FTP Deploy
        uses: SamKirkland/FTP-Deploy-Action@4.0.0
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: ftp
          port: 21
          local-dir: ./          # path to your repo root
          server-dir: /htdocs/   # ⚠️ Must end with a trailing slash
          log-level: verbose
